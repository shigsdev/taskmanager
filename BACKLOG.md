# Task Manager — Project Backlog

Living feature backlog. Updated every session. Claude Code must update this
file whenever a feature is completed or a new issue is discovered. Items are
moved between sections, never deleted.

---

## In Progress

_(nothing currently in progress)_

## Completed

- [x] Project documentation scaffolding — CLAUDE.md, BACKLOG.md, README.md, ARCHITECTURE.md — completed 2026-04-05
- [x] Basic Flask app with Google OAuth login, single user lockdown — completed 2026-04-05
- [x] Database models and migrations (PostgreSQL) — tasks, projects, goals, recurring_tasks, import_log — completed 2026-04-05
- [x] Core task CRUD — JSON API for create, read, update, delete, move between tiers — completed 2026-04-05
- [x] Goals CRUD — JSON API for create, read, update, delete, link to tasks, progress tracking — completed 2026-04-05
- [x] Main UI — tier board, task cards, quick capture bar, detail panel, voice input — completed 2026-04-05
- [x] Goals view — grouped by category, progress bars, filters, linked tasks, create/edit/delete — completed 2026-04-05
- [x] Project grouping for work tasks — CRUD API, seed defaults, project filter, grouped Work view — completed 2026-04-05
- [x] Inbox + triage flow — verified: default tier, single + bulk triage, filtering, complete/delete from inbox — completed 2026-04-05
- [x] Checklist/notes on tasks — verified: CRUD, progress tracking, round-trips, special chars, independence — completed 2026-04-05
- [x] Mobile-responsive CSS — 44px touch targets, swipe gestures, iOS zoom prevention, full-screen detail panel, project filter scroll — completed 2026-04-06
- [x] Weekly review mode — step-through stale tasks, keep/freeze/delete/snooze actions, progress bar, summary — completed 2026-04-06
- [x] Recurring tasks — CRUD API, 16 system defaults (morning/evening routines, day-specific), spawn to Today tier — completed 2026-04-06
- [x] Print view (/print) — server-rendered, Today + This Week + Overdue, checklists, @media print CSS, no nav chrome — completed 2026-04-06
- [x] Email digest — plain-text digest via SendGrid, preview + send-now API, goals summary, overdue alerts, sanitized content — completed 2026-04-06
- [x] Image scan to tasks — Google Vision OCR + Claude API parsing, review screen with edit/deselect, confirm to Inbox, in-memory only — completed 2026-04-06
- [x] Import tool — OneNote task parsing + Excel goals parsing, duplicate detection, ImportLog audit trail — completed 2026-04-06
- [x] Settings page — app stats, service status, digest controls, import history, quick links — completed 2026-04-06
- [x] Security hardening — Fernet encryption, CSP headers, rate limiting, session hardening, auth audit — completed 2026-04-06
- [x] Railway deployment — Procfile, gunicorn config, runtime.txt, deployment readiness tests, full README — completed 2026-04-06
- [x] Bulk-import undo + Recycle Bin (narrow scope) — import-undo only, manual cleanup, batch_id UUID soft-delete, five new routes — completed 2026-04-09
- [x] Photo-to-goal scan — image scan page parses into Goals (not just Tasks) via radio toggle, shared batch_id with ImportLog — completed 2026-04-10
- [x] LOCAL_DEV_BYPASS_AUTH — four-gate localhost-only auth bypass for agent browser testing, triple Railway tripwire, audit trail — completed 2026-04-10
- [x] URL save on tasks — quick-capture auto-detects URLs, server-side SSRF-protected title fetch, clickable link on task card — completed 2026-04-11
- [x] Subtasks (one-level deep) — parent_id self-referential FK, subtask badge on parent card, goal/project inheritance + cascade, force-complete — completed 2026-04-11
- [x] Personal projects — ProjectType.PERSONAL enum, project CRUD type support, detail panel filters by task type, project filter bar in Personal view — completed 2026-04-13
- [x] JS testing infrastructure (Jest phase) — extracted parseCapture to importable module, Jest + jsdom devDeps, 34 unit tests mirroring Python suite, npm test in quality gates — completed 2026-04-16
- [x] Voice memo capture (#9) — `/voice-memo` page with MediaRecorder, Whisper transcription, Claude task parsing via existing scan_service, 10-min cap, per-memo cost logged. Requires `OPENAI_API_KEY` on Railway. iOS Safari Content-Type fix (semicolon + colon codec params) — completed 2026-04-18
- [x] Audit hardening pass — 13 security/correctness findings from 3-agent code audit, all fixed. Includes SSRF DNS-rebinding + redirect-disable defense, Vision API key moved to header, MAX_CONTENT_LENGTH cap, X-Request-ID validation, OpenAI scrub regex, .flaskenv gitignored, voice_memo onerror+visibilitychange race fixes, validator_cookie docstring drift corrected. 4 CVEs patched (flask 3.0.3→3.1.3, cryptography 46.0.6→46.0.7, requests 2.32.3→2.33.0). +9 regression tests — completed 2026-04-18
- [x] Tier 1 gate automation — extended `scripts/run_all_gates.sh` to 8 gates: ruff + pytest + jest + local Playwright + bandit (security antipatterns) + pip-audit (Python CVEs) + npm audit (Node CVEs) + docs_sync_check.py (env vars in code must match README env-var table). Single command, exits 0 only if all green. Replaces "try harder" with automation — completed 2026-04-18
- [x] Tier 2 process additions — CLAUDE.md gained Cascade-check table (10 rows of "if you change X then also update Y") + explicit threat-model section. Both meant to be re-read at session start to keep audit-eye perspective and prevent doc/reality drift — completed 2026-04-18
- [x] Architecture Decision Records (ADRs) — added `docs/adr/` with README + 7 initial records: single-user OAuth lockdown (001), four-gate dev bypass (002), validator cookie narrow scope (003, SUPERSEDED), broaden to read-only routes (004), voice memo error handling (005), SSRF defense (006), API keys in headers (007). Code comments now reference ADRs by number — completed 2026-04-18

## Bugs

_(no open bugs)_

## Backlog (prioritized)

| # | Item | Category | Priority | Value | Effort | Complexity | Status |
|---|---|---|---|---|---|---|---|
| 33 | **OneNote bulk-upload format documentation** — `/import` page needs an inline help panel (or a linked `IMPORT_FORMAT.md` doc) explaining exactly what text shape the parser accepts so the user can clean OneNote data BEFORE pasting | Documentation / UX | Medium | Today the import endpoint accepts pasted text and runs it through `import_service` parsers, but the expected format is not documented end-user-facing. User reports "I don't know how to format my OneNote data to get a clean import." Surfacing the rules (bullet chars that delimit tasks, which lines become notes vs titles, how tiers/due-dates can be embedded, max paste size) lets them fix their source before the import rather than debugging post-import mess. | Low — read `import_service.py` parser logic, write a short doc (probably embed on the Import page as a collapsible "Format help" panel + a dedicated `docs/IMPORT_FORMAT.md` with examples). No code change to the parser itself. | Low — pure documentation. Decision: inline-only vs linked doc? Both is ideal — short summary inline, full examples in the MD. Scope guard: DON'T change parser behaviour; if the docs surface a parser bug, that's a follow-up. | 🔄 IN PROGRESS — code complete, gates green (999 tests), Phase 6 verified desktop+mobile. Scope expanded from the original "inline panel OR MD file" to a **dedicated in-app `/docs` page** per user direction. Two sections shipped: OneNote text import + Excel goals import, each with rules, what-it-doesn't-do, clean-up checklist, worked example, size/undo. `/docs` added to nav; `/import` page has "Format guide →" link to `/docs#import-onenote`. Sticky sidebar TOC on desktop, stacked on mobile. ADR-019. 6 new `TestDocsPage` tests (including verbatim rule assertion as a docs-vs-reality drift guard). Drive-by fix: `compute_previews_in_range` (#32) was bucketing Task.created_at by UTC instead of DIGEST_TZ, which made the same-day-collision filter wrong after UTC midnight on ET evenings — matches the `_local_today_date()` pattern from #28. Post-deploy validate + prod smoke pending. |
| 32 | **Preview recurring templates in This Week panel** (Option A from the 2026-04-20 decision) — render RecurringTask rows as read-only preview cards inside This Week grouped by their fire day, so "what's coming Friday" is visible on Monday without relying on the user remembering the schedule | Feature / UX | Medium-High | Planning gap: today the This Week / Next Week panels claim to show "what's happening this week," but recurring tasks are systematically excluded until their spawn day. A weekly-review scheduled for Friday is invisible on Monday. Pairs with #23 (day-of-week grouping) — a Friday-firing template would render in the Friday sub-group. | Medium — loop `RecurringTask.is_active` and expand `_template_fires_on()` across the next 7 days to compute preview instances per day; client merges preview cards with real Tasks in `renderTierGroupedByDay`. Needs a visual treatment to distinguish "preview" from "real Task" (dashed border, lighter background, 🔁 badge, no tier buttons / no drag). Click opens the template editor, NOT a task detail. No schema change. | Medium — keep preview cards read-only to avoid state ambiguity ("did I complete this future instance or the current one?"). Edge case: if today is the fire day AND the real Task has already spawned, don't double-render. | ✅ Completed 2026-04-20. SHA 57ebcea. New `GET /api/recurring/previews?start=&end=` (7 unit tests: range cap, 400s, daily/weekly expansion, inactive exclusion, same-day spawn collision suppression). `loadRecurringPreviews()` runs in parallel with tasks/goals/projects. `renderTierGroupedByDay` extended to merge previews into day-groups with Treatment A (dashed grey border) CSS. Click opens most-recent spawn's detail panel (where Repeat dropdown edits the template); fallback alert for brand-new templates. `tasks_api._serialize_repeat` now emits `template_id` for the preview→spawn lookup. Day-heading counts exclude previews (user's Option 1 choice — workload honesty). Empty-state bypass for preview-only weeks (Phase 6 catch). Mockup at `docs/mockups/32-recurring-preview.html`. ADR-015. Phase 6 verified desktop+mobile (11+4 in This Week, 14 in Next Week, click flows work, 341px fits 375 viewport). Pre-deploy: 10 gates green, 974 tests. Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 31 | **SQLAlchemy `pool_pre_ping=True` on the engine** — recycle dead connections transparently so Railway's SSL pool blips don't produce user-visible 500s | Infra / Reliability | Medium | Post-deploy observability found `psycopg.OperationalError: SSL SYSCALL error: EOF detected` and `SSL error: decryption failed or bad record mac` intermittently on the first queries after a container starts (and occasionally during normal traffic). They're connection-pool handshakes that silently expired on the server side. With `pool_pre_ping=True` SQLAlchemy sends a cheap `SELECT 1` on checkout and transparently reconnects if the ping fails — the user never sees the 500. The post-deploy log-check gate currently filters these out as transient (commit after `e3f0779`); this item removes the need for that filter by fixing the underlying flake. | XS — one line in `create_app` where `db.init_app` is called (or via `SQLALCHEMY_ENGINE_OPTIONS`). Small perf cost per query (one round-trip) but negligible for a single-user app. | Low — well-trodden SQLAlchemy pattern, no schema change, no migration. Verify via post-deploy log-check: after shipping, the SSL-blip signature filter in `scripts/validate_deploy.py` should become dead code (can remove in a follow-up once we have a few weeks of clean traces). | ✅ Completed 2026-04-20. `SQLALCHEMY_ENGINE_OPTIONS={"pool_pre_ping": True}` in `create_app`. The transient-SSL filter in `scripts/validate_deploy.py` is now belt-and-braces; keep it for a few weeks of burn-in, then remove. |
| 30 | **Parent-task link on subtask detail panel** — when opening a subtask, show a clickable "Parent: <title>" link that opens the parent's detail panel | Feature / UX | Medium-High | Today when you open a subtask, the detail panel hides the entire subtasks section (good — subtasks can't have subtasks) but gives no way to jump back to the parent. You have to close the panel, find the parent on the board, click it. A direct link in the subtask's detail panel removes that friction and makes it obvious which task it belongs to. | Low — `_task_detail_panel.html` already has a `subtask-section` block that toggles on `task.parent_id`. Add a sibling block "parent-link-section" that, when `task.parent_id` is set, fetches the parent's title and renders a clickable link. Click handler opens the parent via `taskDetailOpen(parent)`. | Low — single new DOM block + one fetch. Edge case: parent might be cancelled/archived/deleted — show the link greyed out or with the status badge. | ✅ Completed 2026-04-20. SHA 3cc7ad9. New `.parent-link-section` in `_task_detail_panel.html`, inverse-toggled with the existing `.subtask-section` on `task.parent_id`. `taskDetailPopulateParentLink()` looks up parent in `allTasks` cache first, falls back to `GET /api/tasks/<id>` for archived/cancelled parents. Status badge for non-active parents (archived-green, cancelled-orange, deleted-red). SW cache v43 → v44. ADR-018. 1 new markup test. Phase 6 verified desktop+mobile (link renders, click opens parent detail, archived-parent "completed" badge works, 334px fits 375 viewport, 0 console errors). Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 29 | **Completed section → dedicated page** — `/completed` (or `/tier/completed`) route mirroring #22 for the Completed section on the board | Feature / UX | Medium-High | The Completed section on the board has the same overflow problem that motivated #22 — over time, completed tasks accumulate and the section becomes long. A dedicated page would let you browse history, bulk-restore (via "Mark active" in the bulk toolbar), and search completed tasks in full-page layout. Mirrors the pattern of the 5 tier detail pages. | Low-Medium — one new route (or `name="completed"` special-case in `/tier/<name>`) that filters by `status=archived` instead of `tier=X`. Reuses the tier.html template or a near-copy. Bulk-select + "Mark active" (un-complete) already works from #21. | Low — mostly routing + template work. Decision: add as `/completed` (its own path) or overload `/tier/completed` with a status-vs-tier branch? First is cleaner URL semantics. | ✅ Completed 2026-04-20. SHA 4b16a9b. New `/completed` route (clean URL, NOT overloaded `/tier/completed` per ADR-017). New `templates/completed.html` near-copy of tier.html with `data-archived-list="true"` marker, no `data-default-tier` on capture bar, hint about un-complete workflow. `loadCompletedTasks` now serves both board + dedicated page; new `renderCompletedPage()` uses full `taskCardEl` so bulk-select + click-to-detail work natively. Board's "Completed" heading is now a clickable link. Phase 6 caught + fixed null-guard bug in `updateTodayWarning` (was crashing init on /completed and /tier/<name>). SW cache v42 → v43. ADR-017. 7 new `TestCompletedPage` tests. Pre-deploy: 10 gates green, 992 tests. Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 28 | **Tier → due_date auto-fill** — moving a task to Today sets due_date=today; moving to Tomorrow sets it to tomorrow (when #27 lands) | Feature / UX | Medium-High | Removes a manual step. Today you move a task to Today and separately set its date. Tier semantics already imply a date — the tier IS the date for Today/Tomorrow — so the field should auto-populate. | Low — one branch in `update_task` (and in create_task) that, on tier change, fills `due_date` if currently null. Applies uniformly across all mutation paths (capture bar, detail panel, bulk PATCH, drag-to-tier). | Low-Medium — design question: overwrite existing due_date or only fill if null? Probably FILL-IF-NULL (don't clobber an explicit user choice). Edge case: when moving OUT of Today back to another tier, should the due_date clear? Probably leave alone (user may still want the reminder). | ✅ Completed 2026-04-20. SHA 54f5504. New `_auto_fill_tier_due_date(task, data)` helper in `task_service`, called from both `create_task` and `update_task` after explicit mutations apply. Fill-if-null only — never overwrites an explicit user value; also skipped when the payload mentions `due_date` at all (even setting it to `null` is user intent). Uses `DIGEST_TZ` via new `_local_today_date()` helper so behaviour matches the Tomorrow auto-roll cron (#27). Moving OUT of Today/Tomorrow deliberately does NOT clear due_date. Bulk PATCH covered for free via existing `bulk_update_tasks → update_task` path. No UI/template/CSS change. 11 new `TestTierDueDateAutoFill` tests + E2E verification of 4 curl flows. ADR-016. Pre-deploy: 10 gates green, 985 tests. Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 27 | **"Tomorrow" tier between Today and This Week** — explicit one-day-out planning surface | Feature / UX | Medium-High | Today is "do now"; This Week is "this week broadly." Tomorrow is the bridge — tasks you've decided are next-up but not today. Currently those land in This Week with no day-of-week info, so you can't tell "tomorrow's plan" at a glance. Pairs with #23 (day-of-week grouping). | Medium — `Tier.TOMORROW` enum value, DB migration, board template adds Tomorrow panel between Today and This Week, capture-bar shortcut (#tomorrow), update digest to surface Tomorrow's tasks. | Low-Medium — additive enum + new panel; existing recurring task spawn logic doesn't need changes. Main UX question: does Tomorrow auto-roll into Today at midnight, or stay as Tomorrow until manually moved? Probably auto-roll. | ✅ Completed 2026-04-20. SHA 3050842. New `Tier.TOMORROW` between TODAY and THIS_WEEK. Migration `c5d6e7f8a9b0` (autocommit-block + UPPERCASE per ADR-010). Midnight auto-roll via APScheduler cron at 00:01 local (DIGEST_TZ), using isolated `Session(db.engine)`. Parse-capture gained `#tomorrow` + `#next_week` + `#nextweek` (the #23 row claimed these worked but they'd never been wired — drive-by fix). Parse order changed to longest-first-wins to prevent `#week` matching inside `#next_week`. Digest gains "TOMORROW: N tasks" line. 3 new `TestRollTomorrowToToday` Python tests + 4 new Jest parser tests + 1 more tier parametrized in `test_auth`. ADR-014. Phase 6 verified desktop+mobile. Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 26 | **Subtasks inherit parent's recurring schedule** — when a parent task has a recurring template, its subtasks should respawn alongside the parent each cycle | Feature / UX | Medium-High | Today subtasks are one-shot — when the parent recurs (e.g. weekly review), only the parent comes back; the subtasks stay archived from the previous cycle. That breaks the "weekly review" use case where the same checklist needs to apply each week. | Medium — extend `recurring_service.spawn_due_recurring_tasks` (or wherever the spawn happens) to also clone the parent's subtask list when respawning. Need to decide: clone all subtasks, or only those that were active when the parent last completed? Probably "all subtasks that exist as of the parent's most recent template version." | Medium — touches the recurring spawn logic which is one of the more complex services. Risk: getting the cloning model wrong leads to duplicated subtasks every cycle. Tests must cover spawn + completion + re-spawn. | ✅ Completed 2026-04-20. SHA 1cff271. New `subtasks_snapshot: JSON` column on `RecurringTask`, captured at template create/update, consumed by `spawn_today_tasks`. Only ACTIVE subtasks snapshotted. Spawned subs inherit tier/type/project/goal from the parent that was just spawned; don't inherit notes/checklist/url. Defensive parsing both on write (`_clean_subtasks_snapshot`) and at spawn. ADR-013. 6 new `TestSpawnWithSubtasks` unit tests + 1 `test_repeat_snapshots_active_subtasks` integration test. Phase 6 verified 3-cycle reproduction. Pre-deploy: 10 gates green (963 tests). Post-deploy: SHA match, 8/8 healthz, error log scan PASS, 5/5 prod Playwright smoke. |
| 25 | **Cancelled status for tasks** — separate from "completed" so you can mark tasks you didn't get to without inflating completion stats | Feature / UX | Medium | Honest record-keeping. "Completed" should mean done; "cancelled" means consciously dropped. Without this, you either mark cancelled tasks as completed (skews stats) or delete them (loses the audit trail). | Low-Medium — `TaskStatus.CANCELLED` enum value, DB migration, button in detail panel, optional reason field, surface in completed/cancelled views distinctly, weekly digest counts both. | Low — additive enum value, no breaking changes. Main UX question is whether to show cancelled tasks in the same Done section or a separate "Cancelled" sub-list. | ✅ Completed 2026-04-19. SHA 4cf7692 (with hotfixes for an alembic-transactional ALTER-TYPE bug and a SQLAlchemy enum-name vs .value mismatch — see commit messages 2cf7303 and 9f95b55). Adds `TaskStatus.CANCELLED` + nullable `cancellation_reason VARCHAR(500)` column. Goal progress excludes cancelled from BOTH numerator and denominator (surfaces a `cancelled` field separately so the UI can show drops). Digest gains "PAST 7 DAYS: N completed, M cancelled" line. Reason auto-clears when transitioning out of CANCELLED. New "Cancelled" board section parallels "Completed" (collapsed, lazy-loaded, no drag/drop — restoration requires opening detail panel). Detail panel: ✕ Cancel button + inline reason input. Bulk toolbar: "Mark cancelled" with single shared reason prompt. Belt-and-braces app-startup repair gate (`_ensure_postgres_enum_values`) re-applies the ALTER TYPEs in AUTOCOMMIT every boot. ADR-012. 6 new tests (4 task API + 1 goal progress + 1 digest). Phase 6 PASS desktop+mobile. Post-deploy: 8/8 healthz, 5/5 prod Playwright. |
| 24 | **Project CRUD UI** — page or panel for creating, editing, archiving projects (currently API-only) | Feature / UX | High | Today projects can only be created via the seed endpoint or direct API calls — there's no page where you can add a new project, rename one, archive an old one, or change its color. This blocks customizing the app to your actual work/personal areas. | Low-Medium — `/projects` page (or settings tab), CRUD form, list view with edit/archive controls. CRUD API exists already (projects_api.py), so this is mostly template + JS. | Low — straightforward CRUD UI; main UX question is whether projects live on their own page or as a tab in Settings. | ✅ Completed 2026-04-19. SHA 43e6f0b. New `/projects` page (mirrors goals.html chrome — cards grouped by Work/Personal type, detail overlay for create/edit). Lets the user create, rename, recolor (`<input type="color">`), link to a goal, set sort_order, archive/unarchive. Single soft-delete action exposed as Archive ⇄ Unarchive (DELETE endpoint is identical to PATCH `is_active=false` — surfacing both would be misleading; see ADR-011). Sort: active-first, then `sort_order`, then name. Goals dropdown sourced from `?is_active=all` so existing links to archived goals don't silently drop. Nav-tab inserted between Goals and Import on every authenticated page. SW v39 → v40, `projects.js` added to APP_SHELL + health EXPECTED_STATIC_FILES. ADR-011 documents the page-vs-tab choice and archive-vs-delete merge. 9 new TestProjectsPage route tests. Phase 6 PASS desktop+mobile (0 console errors; create/rename/archive verified end-to-end via real HTTP). Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz, 5/5 prod Playwright smoke. |
| 23 | **"Next Week" tier + day-of-week grouping** — add a NEXT_WEEK enum value to Tier; in This Week and Next Week views, group tasks under day-of-week subheadings (Mon/Tue/Wed/...) based on `due_date` | Feature / UX | High | Better visual planning — see "what's on Tuesday" without digging into individual cards. Forward-looking planning surface (Next Week is missing today, so any task more than ~7 days out has no good home except Backlog). Pairs with #22 (dedicated tier pages). | Medium-High — DB enum migration (Tier.NEXT_WEEK), task_service tier-spawn logic for recurring tasks, board template adds Next Week panel between This Week and Backlog, day-of-week grouping needs `due_date` on every task in those tiers (or fall back to "no date" subheading), recurring-task scheduler updates. | Medium — enum migration is straightforward; the day grouping requires deciding what to do with tasks WITHOUT a due_date (currently common — most tasks land in This Week with no specific date). UX needs to handle the "undated" bucket gracefully. | ✅ Completed 2026-04-19. SHA cbe6510. Tier.NEXT_WEEK enum + Postgres ALTER TYPE migration (SQLite-skip, downgrade no-op). Board order: Inbox → Today → This Week → Next Week → Backlog → Freezer. Day-of-week grouping (Monday-first, "No date" last) on BOTH board panels AND `/tier/<name>` detail pages via shared `renderBoard()` path. `groupTasksByWeekday()` extracted to `static/day_group.js` as a pure function — 9 Jest unit tests (empty, undated, ordering, Sunday-last, malformed, local-time). Local-time date parse avoids UTC rollback for PT users. SW v38 → v39, `day_group.js` added to APP_SHELL + health EXPECTED_STATIC_FILES. ADR-010. Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz checks OK, 5/5 prod Playwright smoke tests pass against live Railway. |
| 21 | **Bulk task operations** — multi-select tasks, then apply one action to all (set type work/personal, assign goal, move tier, mark complete) | Feature / UX | High | Big quality-of-life win — currently every change is one task at a time. As task volume grows, batch operations become essential. Existing inbox triage flow (`bulkTriageBtn` in app.js) is a precedent for the multi-select interaction pattern. | Medium — selection state on task cards (checkbox or shift-click), bulk-action toolbar that appears when ≥1 selected, batch PATCH endpoint for tasks (multi-id payload), confirmation for destructive ops. Reuse existing `triage-check` styling. | Medium — straightforward UI work; main complexity is making the selection model coherent across tier/view filters and not losing selection on re-render. Need to think about whether selection persists across filter changes. | ✅ Completed 2026-04-19. SHA 6a51ff2. "Select" toggle in view-filter bar; sticky bottom toolbar with 6 actions (type, tier, goal, project, complete, delete). New PATCH /api/tasks/bulk endpoint (cap 200 ids, per-task best-effort with `{updated, not_found, errors}` response shape). 11 backend tests. Phase 6 PASS at desktop+mobile. ADR-008 documents single-endpoint + partial-failure choices. Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz, auth preflight PASS, 5/5 prod Playwright. |
| 22 | **Tier overflow → dedicated list page** — click a tier panel header to open a full-page view of just that tier's tasks | Feature / UX | High | Today/Week/Backlog tiers will become long lists. The board view shows a few cards per tier; a dedicated page lets you see the full set without scrolling within a panel. Pairs naturally with #21 (bulk select on the dedicated page). | Medium — new route per tier (or one parametrized route `/tier/<name>`), template that renders just one tier with full pagination, navigation back to the board, links from the tier-header in the board view. Could also support filters (project, type) on the tier page. | Low-Medium — mostly template + route work; existing `task_service.list_tasks` already supports tier filtering. Main UX question: when does the panel automatically suggest "open in full view"? At N items? Always show a link? | ✅ Completed 2026-04-19. SHA 588bfdf. `/tier/<name>` route (5 tiers + 404 on invalid), shared render path via existing `renderBoard()` (zero new render code), extracted `_task_detail_panel.html` partial for reuse, tier headings on board now clickable, capture-bar defaults to current tier, bulk-select + Work-view group-by-project preserved. 5 route tests. Phase 6 PASS desktop+mobile (0 console errors). ADR-009. Pre-deploy: 10 gates green. Post-deploy: SHA match, 8/8 healthz, 5/5 prod Playwright. Pagination deferred (see ADR). |
| 1 | **Playwright browser API testing** — E2E tests for Web Speech, Notifications, SW lifecycle across engines (Chromium, WebKit) | Testing | Medium | Catches browser-specific bugs that Jest/jsdom can't — real SW lifecycle, real speech API, real permission dialogs | Medium — ~500MB browser binaries, new test harness, CI integration | Medium — Playwright API is well-documented but browser permission mocking is fiddly; flaky test risk | ✅ Completed 2026-04-17. 23 E2E tests across 3 spec files. Also fixed app.js null-guard bug on goals page. |
| 2 | **Live post-deploy browser testing** — agent-driven smoke test of deployed Railway URL after every push | Testing / Infra | Medium-High | Proves the *deployed* app works, not just localhost — catches env var issues, CSP headers, real Postgres differences | Low (option 3) to High (option 1) — `/api/test/*` endpoints are a day; full OAuth bot is multi-day | Low (option 3) to High (option 1) — test endpoints are simple JSON; browser session needs cookie mgmt + secret storage | ✅ Completed 2026-04-17 (option 2). `/api/auth/status` + `--auth-check` + 5 Playwright prod smoke tests. Follow-up 2026-04-17: added `flask mint-validator-cookie` (option A) after discovering that Flask-Dance token refresh silently invalidates browser-copied session cookies. Long-lived cookie (`validator_token`, 90-day default) minted offline, narrow scope (auth-status only). Further follow-up 2026-04-17: broadened validator cookie scope to GET-only routes so all 5 prod Playwright tests pass end-to-end. |
| 15 | **Railway Remote MCP integration** — wire Claude Code to Railway's `mcp.railway.com` via OAuth | Tooling / Infra | Medium | Replaces manual `railway run` rituals with in-editor agent flows; enables in-session deploy debugging via `railway-agent` natural-language tool. Most concrete win: validator cookie minting without local Python deps. | Very Low — `claude mcp add railway --transport http https://mcp.railway.com` + OAuth approval. ~5 minutes. | Low — destructive ops are flagged at protocol level so client prompts before running. OAuth scope is per-workspace/project + revocable. | Installed 2026-04-17. NOT YET EXERCISED in a real flow. Next-step test: ask MCP to mint a fresh validator cookie via railway-agent, verify the round-trip works. Then evaluate whether to bake into routine deploy/debug workflow. |
| 16 | **Semgrep gate (cross-platform fix)** — get the Python security-pattern scanner running in `run_all_gates.sh` | Tooling / Infra | Low-Medium | Catches Flask-specific + OWASP patterns that bandit misses. Currently bandit covers ~70% of overlap. | XS — find working PATH (pipx install? brew on Mac?) once and document. | Low — pure config / install path issue; the rule packs are well-maintained. | Deferred 2026-04-18 from Tier 1 work — `pip install semgrep` doesn't put `semgrep` CLI on PATH for Python 3.14 on Windows. Re-enabling instructions left as a comment block in `scripts/run_all_gates.sh`. |
| 17 | **gitleaks pre-commit hook** — secrets scanner in `run_all_gates.sh` (and ideally git pre-commit) | Tooling / Infra | Medium | Catches accidental commits of `.env`, validator cookie files, API keys before they reach git history. Defense in depth on top of `.gitignore`. | S — install gitleaks binary (separate from Python tooling), add gate to script, optionally wire as native git pre-commit hook for catch-before-commit. | Low — well-documented tool, single binary, simple invocation. | Identified in audit-hardening pass 2026-04-18 as Tier 1 but not installed (separate binary, not pip-installable). |
| 18 | **Central egress module** — all outbound HTTP through one `egress.py` helper | Refactor / Security | Medium | Today only `tasks_api.url_preview` has SSRF defense (IP-pin + redirect-disable). Whisper, Claude, Vision callers each implement their own timeout / error-handling. One helper bakes in safe defaults so adding a new external API can't accidentally skip them. | M — extract helper, migrate 4 existing callers (Whisper, 2x Claude, Vision, url_preview), update tests, add ADR documenting the boundary. | Medium — bigger refactor across multiple files; risk of breaking existing tests if helper signature is wrong. Mitigated by mock-replay tests. | From audit-hardening pass 2026-04-18. ROI: makes future external-API integrations safer by default. |
| 19 | **Central upload validation helper** — extract shared MIME / size / empty checks | Refactor | Low-Medium | `voice_api.py` and `scan_api.py` both implement near-identical multipart-upload validation. One helper means new upload endpoints get safety by default and bug fixes apply everywhere. | S — extract to `utils.py` or new `upload.py`; migrate 2 callers; tests already cover the cases. | Low — pure refactor, behavior unchanged. | From audit-hardening pass 2026-04-18. |
| 20 | **Bandit MEDIUM/LOW review** — one-time pass on the 8 LOW-severity findings | Hardening | Low | The gate currently fails only on HIGH+HIGH; LOW/MED findings print but pass. Worth a one-time audit to either fix or explicitly document the exclusions in `.bandit.yml`. | XS — read findings, decide fix vs ignore for each, update `.bandit.yml` skips with reasons. | Very Low — nothing changes in production code unless a finding turns out to be real. | From audit-hardening pass 2026-04-18. |

## Freezer (good ideas, not now)

| # | Item | Category | Priority | Value | Effort | Complexity | Blocked on |
|---|---|---|---|---|---|---|---|
| 3 | **Recycle bin: wide-scope** — route regular task/goal delete through soft-delete | UX | Low | Undo accidental deletes, not just import undos — safety net for daily use | Low-Medium — drop `batch_id` requirement, add soft-delete path, update UX copy | Low — straightforward DB change, but every "delete" button needs UX clarity review | UX design for soft vs. hard delete messaging |
| 4 | **Recycle bin: TTL cleanup** — auto-purge soft-deleted rows after N days | Infra | Low | Prevents DB bloat — set-and-forget maintenance | Low — one APScheduler job, one env var, one UI countdown | Low — simple scheduled query + "expires in X days" UI | Only after manual cleanup proves insufficient |
| 5 | **TLS certificate expiry check** — warn-only check in `/healthz` | Infra | Very Low | Early warning on cert expiry — but Railway auto-renews, so near-zero risk today | Very Low — one function, TLS socket check | Very Low — well-understood pattern, warn-only | Only if moving off Railway-managed TLS |
| 6 | **Doc upload (PDF/DOCX)** — extend `/scan` to accept documents via pdfplumber/python-docx | Feature | Medium | Capture tasks from meeting notes and PDFs without manual retyping | Medium — two new deps, text extraction, Claude prompt tuning, MIME validation | Medium-High — scanned PDFs need OCR fallback (doubles code paths), `.doc` rejection, token cost control | Claude API monthly budget + per-upload char cap |

## Phase 2 Roadmap

| # | Item | Category | Priority | Value | Effort | Complexity | Notes |
|---|---|---|---|---|---|---|---|
| 7 | **reMarkable integration** — USB bridge via remarkable-mcp on Mac | Feature | Medium | Capture handwritten notes to tasks directly from the tablet | Medium — depends on remarkable-mcp maturity and USB reliability | High — third-party MCP dependency, USB quirks, handwriting OCR accuracy varies by writing style | Needs remarkable-mcp stable first |
| 8 | **Native iPhone app** — React Native or Swift | Feature | Medium-High | Faster capture, push notifications, offline support, native feel | Very High — entire new codebase, App Store process, offline sync, push infra | Very High — offline-first sync is notoriously hard, two codebases to maintain | Largest item on roadmap; evaluate after web app is stable |
| 9 | **Voice memo processing** — record during commute, auto-parse tasks | Feature | High | Hands-free task capture while driving/walking — biggest friction point today | Medium — audio recording UI, Whisper/Deepgram transcription, Claude parsing, review screen | Medium — transcription API selection, audio format handling, cost per minute of audio | ✅ Completed 2026-04-18. `/voice-memo` page with MediaRecorder UI, Whisper transcription, reuses `scan_service.parse_tasks_from_text` for Claude parsing, `source_prefix="voice"` in `create_tasks_from_candidates` for recycle-bin distinction. 10-min hard cap, per-memo cost logged. Pre-deploy: ruff PASS, 883 pytest PASS @ 89%, 34 jest PASS, 23 local Playwright PASS. Post-deploy (SHA 157a3e1): 8/8 healthz checks green, auth preflight PASS, 5/5 prod Playwright PASS, /voice-memo serves 200, POST correctly 302 (validator cookie GET-only). Requires `OPENAI_API_KEY` env var on Railway to actually transcribe audio (feature is wired and ships-gated behind that one env var). |
| 10 | **Weekly summary email** — Friday EOD digest | Feature | Medium | End-of-week reflection: what got done, what slipped, what's next week | Low — extend existing digest service with weekly template + Friday APScheduler job | Low — reuses digest infrastructure, just a new template and "completed this week" query | Quick win — builds on existing digest |
| 11 | **iCloud calendar integration** — sync due dates and time-blocked tasks | Feature | Medium | See tasks alongside meetings, block time for deep work | High — Apple CalDAV auth, two-way sync, conflict resolution, timezone handling | High — CalDAV protocol is verbose, Apple OAuth is quirky, two-way sync means conflict resolution | Significant auth + sync complexity |
| 12 | **AI-assisted triage suggestions** — recommend tier based on task age and patterns | Feature | Medium | Reduce inbox pile-up — AI suggests "this has sat 14 days, freeze or delete?" | Medium — data analysis queries, Claude prompt, accept/dismiss UI | Low-Medium — heuristics are simple (age, tier, patterns); main work is suggestion UI | Needs enough historical data to be useful |
| 13 | **Habit tracking streaks** — morning/evening routine completion tracking | Feature | Low-Medium | Visualize consistency, motivate daily habits — builds on existing recurring tasks | Medium — streak model/columns, calculation logic, calendar heatmap or counter UI | Low-Medium — builds on recurring task data; main complexity is streak visualization | UI design needed for heatmap vs. counter |
| 14 | **AI strategy quality checker** — check tasks against goals alignment | Feature | Low-Medium | "12 Work tasks but none link to Q2 OKR goal" — strategic drift detection | Low-Medium — query tasks vs. goals linkage, Claude analysis prompt, summary UI | Low — mostly read-only analysis; hard part is a Claude prompt that gives genuinely useful advice | Interesting but low urgency until goal count grows |

### Quick-win picks (high value, low effort)

1. **Weekly summary email (#10)** — Low effort, reuses digest infra, immediate user value
2. **AI triage suggestions (#12)** — Medium effort, reduces daily friction, leverages existing data
3. **Voice memo processing (#9)** — Medium effort, solves biggest capture friction point
