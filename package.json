{
  "name": "taskmanager-js-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Jest unit tests + Playwright E2E tests for Task Manager JavaScript",
  "scripts": {
    "test": "jest",
    "test:verbose": "jest --verbose",
    "test:watch": "jest --watch",
    "test:e2e": "playwright test --project=chromium",
    "test:e2e:headed": "playwright test --project=chromium --headed",
    "test:e2e:prod": "playwright test --project=chromium-prod",
    "test:e2e:sw": "playwright test --project=chromium-sw",
    "test:e2e:mobile": "playwright test --project=chromium-mobile",
    "test:e2e:local": "playwright test --project=chromium --project=chromium-sw --project=chromium-mobile"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "jscpd": "^5.0.12"
  },
  "comments": {
    "overrides": "Force patched versions of two DEV-only transitive deps flagged high-severity (DoS) by the weekly advisory check. Not shipped to prod — the app runtime is Python, and `npm audit --omit=dev` is already clean. #306: first pinned js-yaml 3.15.0 + brace-expansion 5.0.8. #310 (2026-08-01): NEW advisories landed against those exact pins — GHSA-rgw5-rvv9-x895 (brace-expansion ≤5.0.8, CVE-2026-14257 mitigation bypass) and CVE-2026-59870 (js-yaml !!omap quadratic CPU, 3.x fix NOT backported). Bumped brace-expansion→5.0.9 and js-yaml→5.2.3 (had to leave the 3.x line — no 3.x fix exists). js-yaml 5.x drops the legacy safeLoad API, so verified the dev tree (jest coverage via babel-plugin-istanbul→load-nyc-config, jscpd) still runs green after the bump."
  },
  "overrides": {
    "js-yaml": "5.2.3",
    "brace-expansion": "5.0.9"
  }
}
