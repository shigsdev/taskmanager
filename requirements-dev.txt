-r requirements.txt
# pytest + pytest-cov moved to requirements.txt for #243 (2026-05-27)
# — the /utilities coverage-audit card needs pytest on the Railway
# prod container. They satisfy the dev workflow as transitively
# included from requirements.txt; left as a comment here for the
# bisect-this-file searcher.
ruff==0.6.3
pre-commit==4.6.0
# #274 (2026-05-31): parallelize the pytest gate (run_all_gates.sh). Tests
# are fully isolated (function-scoped sqlite:///:memory: DB per test), so
# `pytest -n auto` is safe and cuts the suite ~214s → ~43s (~5x) with
# identical coverage. Dev/test-only — the Railway coverage-audit card runs
# pytest serially and doesn't need it.
pytest-xdist==3.8.0
