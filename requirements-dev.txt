-r requirements.txt
# pytest + pytest-cov moved to requirements.txt for #243 (2026-05-27)
# — the /utilities coverage-audit card needs pytest on the Railway
# prod container. They satisfy the dev workflow as transitively
# included from requirements.txt; left as a comment here for the
# bisect-this-file searcher.
ruff==0.6.3
pre-commit==4.6.0
